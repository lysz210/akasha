---
# ==========================================
# PHASE 1: System Prep & Docker Installation
# ==========================================
- name: Prepare Pis and Install Docker
  hosts: managers
  become: yes

  pre_tasks:
    - name: Initialize system
      include_role:
        name: system_init

  roles:
    - role: akasha_cloud
    - role: capacnan